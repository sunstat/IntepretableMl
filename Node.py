class Node():

    def __init__(self, point):

        self.point = point
        self.sample_points = None
        self.lime_vector = None

    # subsample points around the Node
    # input: the decay rate
    # return: a list of subsampling points
    def randomSample(decay_rate):
    	pass

        